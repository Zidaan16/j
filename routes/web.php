<?php

use App\Models\Student;
use App\Models\User;
use Illuminate\Support\Facades\Route;

Route::get('/', function () {
    // $user = Student::select()->user()->get();

    // foreach ($user as $key => $value) {
    //     echo "$key: ".$value."<br>";
    // }
});
